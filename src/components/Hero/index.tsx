import React from 'react'
import { HeroSection, FormArea, HeroTitle } from './HeroStyles'

const Hero = () => (
  <HeroSection>
    <FormArea>
      <div className="container">
        <HeroTitle>
          As melhores vagas para tecnologia, design e artes visuais.
        </HeroTitle>
      </div>
    </FormArea>
  </HeroSection>
)

export default Hero
